namespace ASeKi.battle
{
    public class HeroAttributesSystem
    {
        public void CalcCurrentStatus()
        {
            //if(hero == null || hero.HeroData == null)
            //{
            //    return;
            //}

            //int currentHp = hero.HeroData.HP;
            //int currentMaxHp = hero.HeroData.MaxHP;
            //hero.HeroData.BaseStatus.ClearAttr();
            //// 基础属性
            //hero.HeroData.BaseStatus.AddAttr(BaseStatus);
            //// Buff属性
            //BuffManager.instance.AddBuffStatus(hero.ID, hero.HeroData.BaseStatus);
            //hero.HeroData.BaseStatus.Set(constant.StatusTypes.HP, currentHp);
            //// 血量按比例保存
            //hero.HeroData.HP = (int)(hero.HeroData.MaxHP * (1.0f * currentHp / currentMaxHp));
        }
    }
}
