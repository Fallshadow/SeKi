namespace ASeKi.battle
{
    // 帮助判断游戏中的条件是否达成
    public static class BuffCondition
    {
        //public static bool Valify(ulong entityId, Framework.StructCondition buffCondition)
        //{
        //    Entity entity = battle.BattleActorManager.instance.GetActorById(entityId);
        //    return Valify(entity, buffCondition);
        //}

        //public static bool Valify(Entity entity, Framework.StructCondition buffCondition)
        //{
        //    return true;
        //}
    }
}
